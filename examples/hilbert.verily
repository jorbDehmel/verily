// Hilbert-style proof test

// Modus ponens
rule modus_ponens:
  over a, b
  given a, a implies b
  deduce b
;

// Axiom schema 1
rule as1:
  over a, b
  deduce a implies (b implies a)
;

// Axiom schema 2
rule as2:
  over a, b, c
  deduce
    (a implies (b implies c))
    implies
    ((a implies b) implies (a implies c))
;

// Axiom schema 3
rule as3:
  over a, b
  deduce
    (not b implies not a)
    implies
    ((not b implies a) implies b)
;

theorem: a implies a;
