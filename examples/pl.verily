# Propositional logic in Verily
include "pl_rules.verily";

# Propositions
axiom: a in P;
axiom: b in P;
axiom: c in P;
axiom: d in P;

# Value assignments
axiom: a;
axiom: not b;
axiom: c;

theorem: (b implies a) and (b implies c);
theorem: a implies c;
theorem: not (a implies b);
theorem: a implies b or c;
theorem: a iff not b;
theorem: a implies (d or not d);
theorem: d implies a or b;
